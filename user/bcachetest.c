// Placeholder user program for testing buffer cache with locks (LAB_LOCK)
#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

int
main(int argc, char *argv[])
{
  printf("bcachetest: buffer cache lock test program\n");
  printf("bcachetest: test files not yet provided\n");
  exit(0);
}
